<Window x:Class="DictionatiesForEverything.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DictionatiesForEverything"
        xmlns:vm="clr-namespace:DictionatiesForEverything.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <vm:ManageDataVM/>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ComboBox ItemsSource="{Binding AllGlossaries}" Height="30" SelectedItem="{Binding SelectedGlossary, Mode=TwoWay}" Grid.Column="0" Grid.Row="0">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <Label Content="{Binding Path=Name}"></Label>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
        <StackPanel Orientation="Horizontal" Grid.Column="1" Grid.Row="0">
            <Button Content="Добавить глоссарий" Command="{Binding OpenAddGlossaryWindowCommand}"/>
            <Button Content="Удалить глоссарий"/>
            <Button Content="Термины"/>
        </StackPanel>
        <ListBox ItemsSource="{Binding FilteredItems}" SelectedItem="{Binding SelectedGlossaryItem, Mode=TwoWay}" Grid.Column="0" Grid.Row="1">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Label Content="{Binding Path=Name}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <TextBlock Text="{Binding SelectedGlossaryItem.Description}" Background="Blue" Grid.Column="1" Grid.Row="1"></TextBlock>
        
    </Grid>
</Window>
